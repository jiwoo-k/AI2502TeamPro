<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.PostRepository">
    <insert id="save" flushCache="true" keyProperty="id" parameterType="com.lec.spring.domain.Post" useGeneratedKeys="true">
        insert into post
        (user_id, title, content, type)
        values
            (#{user_id},#{title},#{content},#{type})
    </insert>
    <select id="findAll" parameterType="com.lec.spring.domain.Post">
        SELECT
            post.id,
            post.title,
            post.createdat,
            user.name
        FROM post, user
        order by id desc
    </select>

    <select id="findById" parameterType="com.lec.spring.domain.Post">
        SELECT post.id, post.title, post.createdat, user.name, post.content
        FROM post, user
        WHERE post.user_id = user.id AND post.id = #{id}
    </select>
    <update id="update" parameterType="com.lec.spring.domain.Post"  flushCache="true">
        update post
        set
            title     = #{title},
            content   = #{content}
        where id = #{id}
    </update>
    <delete id="deleteById" flushCache="true" parameterType="com.lec.spring.domain.Post">
        delete from post where id = #{id}
    </delete>


</mapper>