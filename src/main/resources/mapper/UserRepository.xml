<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.UserRepository">
    <sql id="SELECT_BASE">
        SELECT
            id,
            username,
            name,
            password,
            juminNo,
            createdAt,
            provider,
            providerId,
            latitude,
            longitude,
            status
        FROM user
        WHERE 1 = 1
    </sql>

    <insert id="save" flushCache="true" parameterType="com.lec.spring.domain.User"
            useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO user(username, name, password, juminNo, latitude, longitude, provider, providerId)
        VALUES (#{username}, #{name}, #{password}, #{juminNo}, #{latitude}, #{longitude}. #{provider}, #{providerId});
    </insert>

    <select id="findById" resultType="com.lec.spring.domain.User">
        <include refid="SELECT_BASE"/>
        and id = #{id};
    </select>

    <select id="findByUsername" resultType="com.lec.spring.domain.User">
        <include refid="SELECT_BASE"/>
        and username = #{username};
    </select>

    <update id="update" parameterType="com.lec.spring.domain.User" flushCache="true">
        update user
        set username = #{username},
            password = #{password}
        where id = #{id};
    </update>
</mapper>